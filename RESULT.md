> Publication context (2026-09-17 UTC): The original report follows unchanged. Statements about "this run/session", verification, and no prior publication refer to its preparation stage. This release performs packaging and hash-integrity checks only. Lean formalization and independent external mathematical review are not completed; final global priority/literature audit remains pending. No full asymptotic solution or prize claim is made. All report-relative file paths resolve under [the frozen evidence directory](evidence/JSP000846_m41_47_20260917/). See [README](README.md) for current publication status and reproduction commands.

# JSP-000846：41 点有限目标的求解结果

日期：2026 年 9 月 17 日。

## 结论

本轮得到可独立重放的计算机辅助结论：

**m(41)=47，等价于 h(41)=6。**

也就是说，41 个顶点的简单图若要同时含有长度 3、4、…、41 的简单环，至少需要 47 条边，而且存在恰好 47 条边的实例。

证据状态：完整结构清单已重新生成；328 个剩余整数问题已被排除；独立 Python 检查器已重放全部排除证书；Z3 对原始 328 个模型也全部返回 UNSAT。尚未完成 Lean 形式化、第三方审查或全球原创性确认。此处“独立”是指不同实现，不是第三方审稿。

这只处理原始 Erdős #1016 的有限参数 n=41，不是证明其一般渐近猜想，也不是已获得奖项。

## 本轮实际完成的工作

| 检查 | 实际结果 |
|---|---|
| 40 点固定阳性控制 | SAT；返回原有正确权重 |
| 41 点固定近似构造 | UNSAT；只缺长度21 |
| 原有九端点结构、释放所有弧长 | UNSAT；Z3 与独立整数搜索一致 |
| Z3 原始模型批次 | 328/328 UNSAT；0 UNKNOWN；0错误 |
| 独立整数搜索 | 328/328 UNSAT，无超时 |
| 排除证书重放 | 328份全部通过，核验20,557个状态 |
| 完整分类重新生成 | 16,824个带标号结构，1,236个旋转/反射类别 |
| 直接结构容量排除 | 908类 |
| 小规模暴力对照 | 3,710个实例全部一致；检查1,355,043个完整赋值 |
| 47边上界 | 39个不同目标长度的显式顶点环全部通过 |
| 证书重新生成 | 328份文件的SHA-256全部与首次输出一致 |

完整分类：

|端点数|全部对称类别|容量直接排除|证书排除|
|---:|---:|---:|---:|
|4|2|2|0|
|5|28|28|0|
|6|153|151|2|
|7|333|289|44|
|8|417|285|132|
|9|224|121|103|
|10|79|32|47|
|合计|1,236|908|328|

## 为什么这不只是“搜索了很多次没找到”

任何可能的46边泛圈图都包含一个41边Hamilton环，剩下恰好五条弦。五条弦最多有十个不同端点。压缩其间的度二路径后，就成为小型端点结构加正整数弧长，总长度为41。共享端点、平行的弦/环弧和不同Hamilton环均未被遗漏。

每个简单环的长度都可以写成“弦数＋选中弧长之和”。某种结构只有38个不同的表达式，就无论如何不可能覆盖39种目标长度。这直接排除了908类。

对剩余328类，本轮新写的整数求解器按目标环长进行分支。每一步都覆盖所有可能实现该目标长度的表达式，再穷尽分配相关弧长。分支会固定更多弧长，因此终止。不能继续的叶节点必须有可复查原因：某个长度已经不可能出现，或剩余表达式总容量不足。

最后生成的不是一句“UNSAT”，而是一份有向无环排除证书。独立Python检查器不调用Z3，不信任C++求解器输出的计数；它自行重建环长表达式、所有分支和每个叶节点的排除依据。

328份证书总大小为403,347字节，包含20,557个经过核验的状态。其中18,378个为排除叶节点，2,179个为分支节点。小证书来自结构推理和整族排除，不是声称只检查两万个完整图就覆盖了所有图。

## 47边的显式图

顶点为0至40。先连接环：0—1—…—40—0，再加入六条边：

(0,20)、(1,10)、(6,32)、(7,9)、(8,14)、(9,12)。

总边数为47。`upper_47_certificate.json`保存了全部39个目标长度的实际顶点序列。检查器逐个核验顶点互异、长度正确、相邻边和闭合边存在。例如7—8—9—7是三边环；0—1—…—20—0是21边环。

五弦底图来自已公开工作报告；加第六条边的上界已经在前一轮试验包中给出，不主张本轮首次发现这一上界。本轮新增的核心内容是完整排除46边情形。

## 数学证明的组成

1. 至多45边不可能：连通图的非零环空间元素数最多为2^(45−41+1)−1=31，不够39种环长。
2. 46边不可能：完整端点分类＋908类容量排除＋328份整数排除证书。
3. 47边可以：显式图及39个环证据。

因此得到本轮的计算机辅助结论m(41)=47。英文`PROOF_NOTE.md`给出了压缩、奇偶递推枚举、容量剪枝与分支完备性的完整论证。

## 如何重放

解压后，在目录内运行：

```bash
python3 check_all.py
```

只需要Python 3.10或更高版本的标准库，不需要Z3、不需要C++编译器，也不需要联网。不要使用`python -O`；检查器会主动拒绝关闭断言的运行模式。

它会从头枚举结构，核验全部证书和47边图，最后输出：

```text
COMPUTATIONAL_PROOF_REPLAY_PASS
```

## 仍未完成的关卡

**Lean：**还需要把分类完备性、压缩等价性、证书检查器正确性与上界核验写成内核可检查的正式定理。当前包没有声称拥有Lean证明。

**外部审查：**三种实现一致不等于第三方审稿；需要独立研究者审查数学归约及检查器。

**新颖性：**本轮查到的公开工作报告仍把h(41)列在5和6之间，没有查到确认m(41)=47的原始证明，但完整专著章节及不易检索的文献尚未核尽，因此不宣称世界首次。

**奖项：**有限参数结果是否符合奖项的实质进展要求需要单独评审。没有提交PR、没有公开发布证明、没有奖金承诺。

本轮运算已完成，未设置会话间自动运行任务。结果、源代码、证书及原始运行记录均保存在本包。
